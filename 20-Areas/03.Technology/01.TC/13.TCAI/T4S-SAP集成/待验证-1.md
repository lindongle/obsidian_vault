---
title: 待验证
updated: 2026-06-06T10:08
created: 2019-05-29T20:40:47
---

==空BOM属性验证，及版本号的更新---只能手动创建bomview。--空BOM不再下发。==
==差异BOM下发，更改单号是否更新修改的行--差异下发的，不同行不同更改单。==
==更改单关联BOM一块下发==
==项目类别LRMN业务逻辑，M项与物理组的关系==

==主数据删除标记设置后，MM03不提示，但状态已经修改，设置状态为空，无法取出删除标识。==
==--对工厂数据打删除标记，不要对集团打==

Part-图纸的关系下发。
选择数据集是否能发与物料的关系
==跟踪链接及属性能否下发--跟踪链接自身的属性可以取出，要在首选项中配置显示跟踪链接的所有属性。==
![image1](a12fc54f573442a6b88b4c25ede7a945.png)
<span style='color:black'>==图纸链接文本（程序/功能）--标准接口没有，波哥提供表==</span>

==更改单的状态，如何设置控制==--更改单两个发送变更的handler，一个01传所有属性，一个02只传状态和更改单号

显示日志报错，但直接输入可以IE访问

==TC中查看SAP数据，提示找不到itemid----需配置E:\PLM\Siemens\T4S\var\mmap\t4s_mapping_config\t4s_prop_mapping.sd==

==设置发布状态映射SAP删除标记---可以==

设置读取首选项的值来获取对照表，然后跟sap做mapping。

Handler 参数验证：

==R项数据无法下发。==

==T文本无号无名称。在BOM中是否下发--可以下发，根据项目类别为T，不判断先主数据，也不接收编号。mapping文件不用改。（如果是T项，单位默认EA）==

==BOM多级创建和单层修改==--PS的ootb的handler，并配合开发handler将target中的目标附件放到解决方案文件夹中。设置两个流程模板。
==验证TC中设置允许空BOM，但未搭建数据，是否可以自动创建空BOM。==--不使用空BOM

==回传PDM 原材料等，采购规范是否回传==
==凭证的全路径是否可以不传？==--只传文件名+后缀

==不安装SAP客户端是否可以传：==
T4S客户端使用分发传，最新版无ICD文件，有OTW安装包，可以拿之前版本的ICD修改使用

邮箱的配置
