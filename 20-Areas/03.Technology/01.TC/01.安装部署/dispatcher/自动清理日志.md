---
title: 自动清理日志
updated: 2026-09-02T16:30:04
created: 2026-07-05T17:04:40
tags:
  - TC安装部署
---



RequestCleanup
RequestCleanup allows for automatic cleanup of completed translation request objects in the database, Staging directory and the logs. It is possible to automatically delete translation requests in both successful (i.e. CANCELLED, COMPLETE, DELETE, DUPLICATE, SUPERSEDED, and NO_TRANS) and un-successful (i.e. TERMINAL) states. RequestCleanup includes removal of any remaining translation request task staging location and contents. There are a total of 6 RequestCleanup properties. An example set of property values is shown below.
The Interval properties specify, in minutes, how often to query for and delete translation request objects. If the interval value is set to zero,translation request cleanup will not be run.
The Threshold properties specify, in minutes, the amount of time that must have passed since a translation request was last modified before it can be deleted.The DeleteLogs properties specifies if the task logs associated with the request being cleaned up should be deleted. True will delete the logs and false will not delete the logs. The example Successful settings below specify that deletion of successfultranslation requests will occur every 5 minutes and that those translation requests last modified more than 5 minutes in the past will be deleted. Task logs will be deleted. The example UnSuccessful settings below specify that deletion of unsuccessful translation requests will occur every two hours and that those translation requests last modified more than 3 days in the past will be deleted. Task logs will be deleted.

RequestCleanup允许自动清理数据库，Staging目录和日志中已完成的转换请求对象。可以在成功（即取消，完成，删除，重复，超级和NO_TRANS）和未成功（即终止）状态中自动删除转换请求。 RequestCleanup包括删除任何剩余的翻译请求任务分段位置和内容。共有6个RequestCleanup属性。一组示例属性值如下所示。
Interval属性以分钟为单位指定查询和删除翻译请求对象的频率。如果间隔值设置为零，则不会运行转换请求清理。
Threshold属性以分钟为单位指定自上次修改翻译请求之前必须经过的时间，然后才能将其删除.
DeleteLogs属性指定是否应删除与正在清理的请求关联的任务日志。 True将删除日志，false将不删除日志。以下示例
成功,设置指定将每5分钟删除successfultranslation请求，并且将删除上次修改过去超过5分钟的翻译请求。任务日志将被删除。
不成功设置指定将每两小时删除不成功的翻译请求，并且将删除上次修改过去3天以上的翻译请求。任务日志将被删除。

D:\Apps\Dispatcher\DispatcherClient\conf\Service.properties

Service.RequestCleanup.Successful.Interval=60

Service.RequestCleanup.Successful.Threshold=480

Service.RequestCleanup.Successful.DeleteLogs=true
--改为true
Service.RequestCleanup.UnSuccessful.Interval=120

Service.RequestCleanup.UnSuccessful.Threshold=2880

Service.RequestCleanup.UnSuccessful.DeleteLogs=true

--不删除失败的请求日志
Service.RequestCleanup.UnSuccessful.Interval=0

Service.RequestCleanup.UnSuccessful.Threshold=28800

Service.RequestCleanup.UnSuccessful.DeleteLogs=false

