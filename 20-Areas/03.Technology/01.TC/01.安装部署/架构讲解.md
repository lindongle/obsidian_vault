---
title: 架构讲解
updated: 2026-07-31T10:03:30+08:00
created: 2019-03-15T21:56:08
tags:
  - TC安装部署
---

2层
FMS文件
卷服务
数据库
Tcdata
BMIDE
客户端---TAO--tcdata--数据库，tccs--FMS上传下载

4层
Pool池---weblogic，J2EE服务器---Poolmananger--tcserver，白天节点30，晚上10。最大数量100.
Weblogic--tcweb层--tc.ear/war--ThinClient

企业层---企业层--2层客户端--2层BMIDE--==FMS文件缓存-==--变更管理、配置管理、时间表集成。。。NX集成、CATIA集成---配置要求很低---Tcdata--连接数据库
卷服务器---Volume --主FMS服务--卷路径（共享存储）--tcdata（含数据库连接、license连接、单点登录）
---配置要求低---硬盘要求比较大
数据库服务器
---Linux+oracle（oracle软件+数据库文件---共享存储）
Web/pool服务器，安装Fundation--连tcdata--连主FMS--安装的模块（跟模型进入数据库有关的，必须跟企业层一样，变更管理、配置管理、CATIA集成等）
FMS缓存（传输卷）——J2EEE（POOL池）--webtier（瘦客户端+分发（分发服务+分发实例（多个）））
---配置要求很高，一个session200M，空闲50M。--硬盘要求不太多（缓存）
Dispather（ETS）--调度服务（三维转JT--WORD转PDF--word转项目里---流程后台运行（handler--下发SAP、归档））+**计划**（排队，license点数）+**转换模块（model上述）**+转换客户端（登录--转换-日志--缓存处理--dispatchadmin） +JT转换
--配置要求高，硬盘较低

license服务器---稳定（硬件要求不变动）getcid--配置要求很低，可连接。

另一台dispatcher

双机热备（FMS、数据库（RAC）、WEB（负载均衡，单独一台weblogic管理控制器））

传输卷连接主卷
--backup_xmlinfo导出卷信息（web层上执行）--根据SID、卷ID、传输卷ID--更新主FMS（卷服务器）tc_root\fsc\fscmaster开头。重启主FMS服务，去web层重启非主FMS服务（自动将fscmaster配置文件加载过来）

license多台配置
1、TC10及之前--部署多台服务--修改tcdata\tc_pro中的licenseserver，ip@28000,中间分号隔开。
2、TC11之后，部署多台服务，组织--许可证服务器，添加，输入另一台信息---选择用户--下拉选择对应的license服务器。

2层--服务在客户端（plugins和bin）服务端更新
4层，只在服务端更新bin（dll文件），客户端在分发中更新plugins中的jar包。rac文件夹--2层的portal文件夹--更新。

Vis---base/standard/pro/mockup---DMU---Digital Mock Up
VM--虚拟

JDK和JRE--只有安装weblogic会用到JDK，其他均安装JRE。但是所有服务，均需要添加JRE64_HOEM和path中添加JRE\BIN目录。

先后顺序
1、数据库
2。企业层--tcdata
3、其他无顺序

打补丁--按照顺序打。多个补丁，不行均达成一个补丁，再顺序打第二个补丁。
另外，数据库装完，必须逐个按照，不允许多台服务器，一块同时安装应用服务。

