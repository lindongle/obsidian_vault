---
title: 内部讨论
updated: 2026-06-06T10:08
created: 2018-07-09T12:53:19
---

![image1](f0528541d62a40ad800d0e9f8f0922cb.png)
龚俊：IT线
陈俊：业务
吴奇峰：项目管理
嘹亮、张永志，30号前虚拟评审，一个单车

英文系统，location中文
VAS，增加VB，虚拟样车
弄清：第二个分支节点的上层结构。

VAS，全部克隆（除IA），模板不包含IA。
体外确定用到的IA，克隆后手动建立IA。产品结构中挂到工程分支下。更改IA设计权限给工程师。

造型分支：搭建结构，外饰面、内饰面，更改结构权限
总布置：分支结构参考蓥石。
study：到VPPS第二层。第三层根据方案不同建立不同的IA。方案确定后，数模放到正式工程分支下的IA中，但只是数模一样，item号不同。--做配置、方案对比
VA、VB：模板到第二层，跟工程一样。将需要做虚拟评审的IA（单一配置）放到VA对应的位置，完成后从顶层做精确装配。多次虚拟评审，两种方式：多个版本或多个号。IA上几层都没有基线。

IA下只有product，没有part，上几层均没有数据集。IA位置在IA的product的绝对坐标。
配置放到IA下一层上。

发布状态：
IA以上的层级，之定义一个发布状态，C/R。
IA及以下状态，0,1,2,3
图纸管理：
004版本状态为3
单独图纸item，前缀dwg。确认精确还是非精确。处理图纸上标题栏上的版本号，必须与item版本号一致的问题。
dwg版本下必须有product，通过集成环境，将对应的三维item装配过去。
自动转PDF

左右件：共图，dwg装配两个。出一张图。catia自己控制关联关系及控制显示哪个图的二维图。
变形件：单独item，编号规则。自定义关系。原型件，不再VAS上体现。只变形件在VAS上。原型件。
post件和参考件都不用。
0状态发布：新建流程，中间时，先发布成中间状态，如0_wip。再通过EWO发布成0状态。

EBOM：
<span style='color:black'>UPC:</span>
EBOM-VPPS1-VPPS2 模板。
<span style='background:yellow;mso-highlight:yellow'>零件属性：</span>
<span style='color:black'>==BOM属性：UPC/FNA，VPPS号库。分类库中建立好。创建IA下级零件时，关联UPC/FNA库，选择好后，当做零件名称。--了解关联UPC/FNA结构，并在属性上体现出VPPS。选择的时候将BOM属性关联过去。使用分类节点的OOTB预留的属性名称。==</span>
<span style='color:black'></span>
<span style='background:yellow;mso-highlight:yellow'>链接关系：</span>
<span style='color:black'>==编号：IA以上层级，PART和设计零件都一样，可以一下克隆两个。--二次开发。==</span>

IA及以下零件：创建part时，勾选设计。自动创建设计item。--检查OOTB是否可以自动创建设计item
part下把二维图关系手动粘贴过来。确认是否有Document？
==创建设计item时。点击初次同步，自动创建对应的part零件。==--二次开发

part未发布，设计关联最新版本。
part和设计升版和创建的时候做什么扩展规则来控制。

part没有单独的发布流程。通过EWO
design有单独的发布流程，只发布到最新状态。

变更：
版本-变更前版本（part版本），变更后版本（part版本），图纸（放所有到中间发布状态，如0-wip），数模（放所有到中间发布状态，如0-wip）。==变更走完后，将中间状态标记去掉，0-wip到0,1-wip到1.二次开发==handler。

检查状态的升级，下级状态必须等于或大于父级状态。

EWO中数模或图纸发布了才能发布零件。
零件新增、变更的流程均通过EWO创建--ECN对象
<span style='color:black'>==EWO变更前后对比--二次开发==</span>
确定好EWO流程模板

配置：
车型配置表
整车级别选项：2018年车，销往欧洲，豪华型等
功能技术选项：座椅、发动机、变速箱，多少IA多少选项，包括强制性选项，和可选选项。
选项值：1.5T/2.0T，AT/MT等
以上两个选项形成企业级别字典库。

约束关系

DBOM 只做虚拟评审，只做整车级选配。放在IA下级。
EBOM，整车级功能级全使用。

市场选项、技术选项、制造选项。

通过一对一、一对多自动驱动。

关系下的限制分析。

<span style='color:black'></span>

