---
title: 集成内容
updated: 2026-06-05T22:02
created: 2018-11-19T18:38:45
---

**集成内容（数据）:**
全局特征库PDM系统同步接收BOM系统的特征族、特征值库信息

<span style='background:yellow;mso-highlight:yellow'>具体内容：全局配置字典（库编号、库名称、分组编号、分组名称、特征编号、特征名称、特征值编号、特征值名称）</span>
车型特征清单PDM系统同步接收BOM系统的车型特征清单信息
具体内容：
==车型配置关联：车型代号、名称或与TC中整车的映射关系、分组编号、分组名称、特征编号、特征名称、特征值编号、特征值名称==
工程配置表PDM系统同步接收BOM系统的车型配置信息
具体内容：
==配置单：车型代号、配置特征及特征值、对应BOM行信息（父子代号），对应的车型是否需要先提前在TC中建立或接收。==
整车结构整车结构规范信息（VBS）
具体内容：
==整车结构到哪一层？包含哪些具体信息（零件属性、BOM属性）与TC中整车结构的区别是什么？==
«span style='font-family:微软雅黑;background:yellow;mso-highlight:yellow'»零件位置库«/span»<span style='font-family:Calibri;background:yellow;mso-highlight:yellow'>PDM</span>«span style='font-family:微软雅黑;background:yellow;mso-highlight:yellow'»系统同步接收«/span»<span style='font-family:Calibri;background:yellow;mso-highlight:yellow'>BOM</span>«span style='font-family:微软雅黑;background:yellow;mso-highlight:yellow'»系统的零件描述库（«/span»<span style='font-family:Calibri;background:yellow;mso-highlight:yellow'>SPC</span>«span style='font-family:微软雅黑;background:yellow;mso-highlight:yellow'»、«/span»<span style='font-family:Calibri;background:yellow;mso-highlight:yellow'>FSA</span>«span style='font-family:微软雅黑;background:yellow;mso-highlight:yellow'»、描述）«/span»
==GBOM传的整车模板是EBOM还是 DBOM，DBOM克隆后的BOM是否回传给GBOM？==
具体内容：
UPC/FAN的编号及名称（中英文）、层级关系
==修改后的更新机制？是否不判断权限直接更新BOM属性位置码对DBOM的作用是？一般都在EBOM的结构属性上。==
零件名称库PDM系统同步接收BOM系统的零件名称库（分组号、顺序号、名称）
<span style='background:yellow;mso-highlight:yellow'>名称库的结构形式及层级？与整车的结构区别？，包含的数据（中英文名称？）</span>
<span style='background:yellow;mso-highlight:yellow'>名称库只给变形件或扩展件用吗</span>
零件申请PDM系统同步接收BOM系统申请的零件对象（含基本信息）
==零件申请单的信息是否需要保存到TC中？零件属性哪些？BOM属性，如位置信息不能通过此接口传递。==
零件设计属性信息PDM系统回传数模、图纸发布后的版本信息，以及可直接从数模中读取的零件设计信息
<span style='background:yellow;mso-highlight:yellow'>哪些属性？回传直传数模和图纸的版本最后一个属性？数模及图纸实体文件是否回传？从数模读取零件设计信息是什么意思？如果数模不会传的话。传实体文件还是路径等链接关系。</span>
==零件属性接过来放设计Item版本上还是单独Part对象？如果放设计版本上，那接完之后的属性权限如何控制？设计属性回传GBOM时，是否还将零件属性一块回传？==
==零件属性传递给TC后，状态权限如何控制？==
零件结构信息PDM零件结构发布后回传零件结构信息
==结构属性哪些？父子代号在TC中不存在如何处理？需要回传哪些数据和属性？==
==回传时是否更新EBOM，GBOM中两套BOM是否都保留？==
==IA以下的结构变更，两边各自修改还是，DBOM修改后更新EBOM，BOM属性权限如何控制？==
==是否存在IA下级总成的结构EBOM和DBOM不一样的情况？变形件？颜色件？==
==BOM下级过滤焊点、设计胶，总成BOM就不一致了。==
==变形件如何回传？在EBOM中是否跟本件是一个物料号？==
数模基线版本信息（TG2之后）PDM系统回传数模更改后的基线版本信息（TG2之后的基线版本信息）
==TG2之后（前还是后，如果直传基线版本号，没啥意义）回传基线版本对应的BOM及零件信息？GBOM中如何存储及展示？传输哪些零件属性及BOM属性？多个基线版本每个都要回传吗？根据基线版本关联每一层级BOM的逻辑（Item版本属性会存放每个版本对应的子件信息）？基线版本只传递整车级别还是每个零部件的基线版本都要回传？==
设变结果同步PDM将设计变更结果同步到BOM系统中
==设计变更的信息是否还要回传，如变更单号等、BOM变更传递最新版本全BOM？传递的BOM属性是哪些？还是只传递是否完成变更的状态？==
设计变更申请PDM将OA设计变更信息同步到PDM中
==设计申请单是否在PDM创建对象？设变申请单中的新的零件是否在GBOM中先创建？==

集成需统一点：
1）双方传递的数据及属性是否在双方系统中存在对应模型及属性，不存在需添加。
2）每个对象接收存在性检查及对应逻辑判断处理。接收方考虑。
3）手动接收/发送还是自动接收/发送
4）接收的位置存放在哪？
5）接收或发送的日志接口是否需要定义？
6）接收或发送后的通知？

**集成方式：ESB**
==接口谁来定义？按照谁接收谁定义的方式？具体技术是什么？函数还是json？较大数据量处理的机制是什么？分批传送？接口都通过ESB吗？==
集成触发时间及频率：
1、人工手动触发菜单发送/接收
2、发布后自动发送/自动/手动接收，如自动，发送前校验过程错误如何处理？
3、哪些定时发送？发送的频率及触发，错误反馈？
4、如果都实时，考虑数据量较大接口导致流程发布崩溃问题。
三方职责的明确：
1、哪些接口哪方负责编写？另一方负责调用，接口实现方提供具体传入传出参数的属性、类型、长度等信息。
2、接口方案三方签字
3、双方提供的数据结构模型或模板。仅作参考，已实际接口函数为主。

PPT中3页 LOU-LOA quantity是什么意思？
PPT中 6页 BOM行和并、表单什么意思？
配置数据库传递业务，配置变更集成？
基线传输的作用

零件传输后名称/名称不能在PDM中改。bmide设置只读。
零件属性放到主item上。编号、名称、需要确认GBOM不能改名称。

