---
title: 文档与知识库
updated: 2026-06-05T22:03
created: 2019-05-05T12:52:18
---

文档的四类文档--标准类、项目交付物、交付物模板、部门内部文件
1、层级如何划分（文件夹结构）
2、每层文件夹创建的角色如何定义
3、每个底层文件夹中放入哪类的文件
4、文件的模板梳理。
5、根据TC账号的权限及文档访问共享，切割哪些文档纳入TC管理。

自动归档，梳理规范类文档与交付物文件夹结构的一一对应规律。
后续可以根据需要扩展文件夹层次结构。
文档入库时定义标签。便于后续检索。
交付物层次结构--多流程，并列放多项目结构。、

不同版本不同发布状态的权限如何控制（可见）

PDF生成时，每一步生成PDF，发布后统一签名。

创建item时，自动关联引用对应的技术文档。--建立一一对应关联。对应关系细化到哪一层，是否每个零件都要维护其对应技术文档？

文档类型分几种，需要定义。
新建文档时调用实体模板。
